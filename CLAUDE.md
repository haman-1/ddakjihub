# 딱지허브 — 프로젝트 가이드

한국어 반려동물(개 중심) 정보 사이트. **품종은 대표 축이지 전부가 아니다** — 반려동물과의 일상, 관리·건강·용품 등 반려동물 전반의 에버그린 정보를 다룬다. **최종 목표: AdSense 광고 수익 + 제휴 수익(쿠팡파트너스 등 — 반려동물은 사료·용품 제휴가 잘 붙는 니치).**
**사이트: 딱지허브 — https://ddakjihub.com/** (반려견 '딱지'에서 지은 이름, 개 중심 시작)

진행 상황·할 일은 `PROJECT-STATE.md`를 먼저 읽는다.

## 사이트 뼈대 (2026-09-09 재구축 — 이 사이트 목적에 맞게 새로 설계)

- **테마 없는 Hugo 사이트** — 레이아웃은 `layouts/`, 스타일은 `assets/css/main.css`가 전부. 외부 테마·서브모듈 없음. 뼈대는 이 사이트에 맞춘 것만 쓴다 — 다른 사이트의 구조·패턴을 가져오지 않는다
- **한 글 = 한 디렉터리(페이지 번들)** — `index.md`와 이미지를 같은 디렉터리에 둔다. 글이 수백 편이 되어도 글별 자산이 흩어지지 않는다
- **섹션 = 콘텐츠 유형**, URL은 디렉터리 구조 그대로:

  | 유형 | 경로 | URL |
  |---|---|---|
  | 품종 허브 | `content/breeds/<견종슬러그>/index.md` | `/breeds/<견종슬러그>/` |
  | 스포크 | `content/breeds/<견종슬러그>-<주제슬러그>/index.md` | `/breeds/<견종슬러그>-<주제슬러그>/` |
  | 실용 가이드·리뷰 | `content/guides/<슬러그>/index.md` | `/guides/<슬러그>/` |
  | 일상·경험 | `content/daily/<슬러그>/index.md` | `/daily/<슬러그>/` |

  - 스포크는 허브 디렉터리와 같은 접두사로 짓는다(예: `border-collie/` 허브 + `border-collie-colors/` 스포크) — 파일 탐색기에서 같은 견종끼리 붙어 정렬된다
  - 리프 번들 안에 다른 번들을 중첩할 수 없다(Hugo 제약) — 이래서 스포크는 형제 디렉터리 규칙
- **디렉터리·파일명은 반드시 영문** — URL이 디렉터리명이 된다
- **series 택소노미** — 허브와 스포크에 같은 견종 영문 슬러그(`series: "border-collie"`)를 넣으면 본문 하단 '관련 글'이 자동으로 붙는다. 허브-스포크 내부 링크의 뼈대
- **새 글은 아키타입으로 시작** — 유형별 템플릿이 내장되어 있다:
  ```
  hugo new -k hub   breeds/<견종슬러그>/index.md
  hugo new -k spoke breeds/<견종슬러그>-<주제슬러그>/index.md
  hugo new -k guide guides/<슬러그>/index.md
  hugo new -k daily daily/<슬러그>/index.md
  ```

## 핵심 전략 (2026-09-09 기획 확정)

- **에버그린 콘텐츠** — 뉴스성 없음. 발행 후 몇 달~몇 년 검색되는 자산형 글만
- **롱테일 키워드만 노린다** — "보더콜리" 단독은 대형 사이트·유튜브가 잡고 있어 승부하지 않는다. "견종명 + 색상 종류 / 운동량 / 아파트 / 털 관리 / 사료 / 분양가" 형태로 진입
- **허브-스포크 구조** — 품종 허브 1개가 털색 변형·운동량·훈련·사료·질병 스포크 5~10편을 낳는다. 순수 품종 소개만 하면 공인 견종 200여 종을 3~6개월 만에 소진하므로, 처음부터 이 구조로 설계해야 몇 년 굴러간다
- **차별화 축 3개** (이것 없이는 "정보 나열"이 되어 실패):
  1. **한국 실정 데이터** — 분양가대, 아파트 적합성, 한국에서 흔한 유전병, 장모종 여름나기 등. 해외 사이트가 못 채우는 영역
  2. **털무늬·색상 변형 심층** — "보더콜리 색상 종류"(블랙화이트·블루머르·레드머르…)처럼 실제 검색되는 서브 키워드. 품종별 시그니처 콘텐츠
  3. **개인 사진 + 경험 기반 서술** — 운영자 본인 반려견 사진과 실제 키운 경험. AI 나열 콘텐츠와 확실히 갈리는 E-E-A-T 요소

## ⚠️ 반드시 지킬 것 (리스크 대응)

- **"정보 나열" 금지** — 견종 기본 정보는 위키피디아·AKC·기존 반려견 사이트에 넘쳐난다. 요약 나열이면 구글 scaled content abuse 정책에 걸리고 AdSense도 리젝된다. 모든 글에 차별화 축 중 최소 1개를 얹는다
- **신규 도메인 초반 1~2개월은 편수보다 질** — 첫날부터 하루 2편씩 AI 콘텐츠는 스팸 패턴으로 보인다. 초반에는 개인 사진·경험 콘텐츠 비중을 높여 "사람이 만든 사이트" 증거를 쌓는다
- **AI 이미지는 실사(사진) 스타일로 생성한다** (2026-09-10 방식 변경 — 일러스트에서 실사로) — 머르·브린들 같은 복잡한 털 패턴은 틀리게 나오기 쉬워 생성 뒤 반드시 한 장씩 검수하고, 틀리면 재생성한다. 본인이 찍지 않은 실제 사진은 쓰지 않는다
- **출처 없는 사실 금지** — 건강·질환 정보는 특히 신중. 유전병 정보는 수의학적 일반 자료(건국대 수의과대학 반려동물질환연구소, AKC 등)에서 확인. 확인 안 된 숫자·가격은 쓰지 않는다
- **타인의 사진 무단 사용 금지** — 개인 사진은 본인이 찍은 것만

## 콘텐츠 규칙

**콘텐츠 유형 4종** — 품종 허브(검색 유입 엔진, 1품종 1글) / 스포크(허브에서 뻗는 단일 주제 심층: 색상 변형·운동량·산책·훈련·사료·유전병·아파트 적합성 등) / 일상·경험(E-E-A-T와 AdSense 심사의 얼굴) / 실용 가이드·리뷰(제휴 링크가 붙는 비교·추천 글 포함)

**품종 허브 템플릿** (아키타입 `hub`에 같은 구조가 내장):
1. 빠른 답 (핵심 질문 3개 즉답: 성격 한 줄 / 아파트 키우기 가능 여부 / 처음 키우기 어려운지)
2. 품종 개요 (역사·용도 기원 2~3문단 + 표: 크기/체중/수명/운동량/털 관리 난이도/초보 적합도)
3. 성격과 기질
4. 털무늬·색상 변형 (품종별 시그니처 섹션 — 별도 스포크 글로 뽑을 소재)
5. 한국에서 키울 때 (분양가대, 아파트 적합성, 여름 관리, 흔한 유전병)
6. 관리법 (털·운동·훈련)
7. FAQ 3~5개
8. 출처 (내부 링크는 series가 자동으로 붙인다)

**프런트매터:**
- 공통: `title` / `date` / `description`(150자 내) / `summary` / `categories` 1개(품종·일상·관리·훈련·건강·용품) / `tags` 2~3개(견종명 포함) / `draft: true`(검수 후 false)
- 허브·스포크만: `kind: hub|spoke` / `breed`(한글 견종명) / `series`(견종 영문 슬러그 — 허브·스포크가 같은 값)
- 가이드·일상: `kind: guide|daily`

**형식:**
- 글 첫머리에 "**YYYY년 M월 D일 기준**" 명시 (분양가·제품 가격 등 시간이 지나면 변하는 정보 포함 시)
- 비교·수치는 반드시 표로, FAQ는 스키마 마크업 감안한 질문형 소제목
- 검색 의도 섹션 구성: 같은 품종이라도 "색상"으로 검색하는 사람과 "사료"로 검색하는 사람의 글이 다르다

## 한국어 문체

- 원어민이 쓰는 매끄러운 문장이 우선. 번역체 점검 — 영어 어순 그대로 옮긴 수식구, 불필요한 피동, 긴 명사사슬은 조사로 끊는다
- 어색한 구어체 어휘 금지 — 중립적 표준 표현 사용
- 완성 후 소리 내어 읽었을 때 걸리는 문장은 고친다
- **글뿐 아니라 이 프로젝트의 모든 한국어(페이지 문구, 히어로, 메뉴, 안내 텍스트, 커밋 메시지)까지 자연스러운 한글로**

## 내부 링크 플라이휠

- 품종 허브 ↔ 스포크: `series`가 같으면 본문 하단 '관련 글'이 자동 생성 — 다만 본문 안에서도 주제가 이어질 때 한두 번 링크를 직접 넣는다
- 비교 글("보더콜리 vs 웰시 코기 — 아파트에 더 맞는 견종은?")은 guides에 두고 양쪽 견종을 태그로 붙인다 — 양쪽 허브에서 링크받는 허브급 소재

## 제휴 콘텐츠 규칙

- 제휴 링크가 포함된 글은 첫머리에 공개 문구 필수 ("이 글에는 제휴 링크가 포함되어 있습니다")
- 제휴 링크는 리뷰·비교·추천 글(guides)에만. 순수 정보 글(품종 소개 등)에는 붙이지 않는다
- 실제 사용·비교 기반으로만 추천

## 이미지 운영

- **이미지는 그 글의 디렉터리 안에 저장** — 예: `content/daily/first-bath/cover.jpg`를 본문에서 `![](cover.jpg)`로 삽입
- **표지 규약**: 글 디렉터리에 `cover.jpg`(또는 png·webp)를 넣으면 글 상단 표지와 목록 카드 썸네일(16:9 잘림)로 자동 표시된다 — 프런트매터 설정 불필요
- **개인 사진**: 본인 반려견 사진 — 경험 기반 글의 핵심 자산. 무단 사용 금지
- **AI 이미지**: **Gemini 이미지 생성 API로 직접 만들어 글에 붙인다** (2026-09-09 확정, 2026-09-10 실사 스타일로 전환 — 운영자가 AI Studio 화면에서 수동 생성하던 계획은 폐지). 색상 도감처럼 사진이 설명의 핵심인 글은 색상 수만큼, 일반 글은 1~2장
- **API 키는 루트 `.env`에만 보관** (gitignore 처리됨) — 이 저장소는 공개 저장소이므로 키가 커밋되면 절대 안 된다
- 프롬프트에 품종 특징(색상·체형)을 구체적으로 명시. 생성물은 발행 전 사람이 확인 — 품종 특징이 틀리면 재생성
- 생성 스크립트: `scripts/generate-image.ps1` (키는 `.env`에서만 읽음, 모델·비율 지정 가능)
- **2026-09-10 확인**: 이미지 생성 API는 무료 할당량이 없다(전 이미지 모델 한도 0, 429) — Cloud 결제 연결이 필요. 문서상 단가는 `gemini-3.1-flash-image` 1장 $0.067 안팎이지만 **실제 청구는 장당 약 510원** — 예산은 실측값 기준으로 잡는다. 사이트 표지는 1K로 충분하다
- **이미지 생성 요청은 한 번에 한 장만, 같은 이미지는 중복 없이 딱 한 번만 생성** (사용자 지시 2026-09-10) — 모델은 `gemini-3.1-flash-image` 고정. 생성 → 검수 → 통과하면 다음 장. 여러 장을 루프로 돌려 일괄 생성하지 않고, 검수 결과가 나빠도 승인 없이 재생성하지 않는다

## 기술 주의사항

- Hugo 0.165 Extended, 테마 없음. partials는 `layouts/_partials/`(신규 디렉터리 구조)
- Hugo 신엔진 자동 이스케이프: `{{ .Title }}`처럼 베어로 두면 알아서 처리 — `| html` 파이프를 덧붙이면 이중 이스케이프 버그(`&#34;` 노출)
- 스타일은 `assets/css/main.css` 한 파일 — `resources.Get`으로 번들·지문화됨. `DESIGN-aspca.org.md` 시안 적용본(2026-09-09)
- 설정 키는 `locale`(Hugo 0.158부터 `languageCode` 폐기)
- 미리보기: `hugo server -b http://localhost:1313/` — `-b` 없이 띄우면 baseURL 플레이스홀더 때문에 CSS·링크가 전부 404. 드래프트 포함 미리보기는 `-D` 추가
- 발행 전 `hugo` 빌드로 검증 (에러·경고 없어야 함)
- 배포: push → GitHub Actions 자동 빌드·배포 (`.github/workflows/hugo.yml`, Pages Source=GitHub Actions)

## 디자인

- 기준 문서: `DESIGN-aspca.org.md` (2026-09-09 채택 시안 — 사용자가 새 자료를 주면 그것이 우선)
- **시안 적용 완료(2026-09-09)** — `assets/css/main.css`가 이 시안으로 작성됨. 색·radius·타입 값은 문서 기준값을 따른다(한국 가독 조정분은 CSS 상단 주석에 기록)

## 검색엔진·수익화 로드맵 (상세는 PROJECT-STATE.md)

사이트 구축 → 필수 페이지(about/privacy/contact) → 초기 콘텐츠 10편+ → GSC·네이버 서치어드바이저 등록 → 콘텐츠 축적 → 글 20~30개 + 플레이스홀더 교체 후 AdSense 신청 → 쿠팡파트너스 승인 후 제휴 병행

## 글 추가 요청을 받으면

리서치(원 출처 확인) → 사실 검증 → `hugo new -k <유형>`으로 시작해 작성(템플릿·문체 규칙) → `hugo` 빌드 검증 → 사용자 검수 대기 (`draft: true`).
